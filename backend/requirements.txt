# Backend (FastAPI inference service) dependencies.
# This service ONLY loads versioned ML artifacts and serves inference --
# no training/sklearn-fitting code lives here. The sklearn/xgboost/shap stack
# is needed at runtime purely to DESERIALIZE the pipeline artifact and run
# predict_proba + SHAP explanations.

# --- web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
python-multipart==0.0.20          # OAuth2 form login

# --- persistence ---
SQLAlchemy==2.0.37
alembic==1.14.1                    # migrations (schema mirrors backend/database/models.py)
psycopg2-binary==2.9.10            # PostgreSQL driver (prod); tests/dev default to SQLite

# --- auth ---
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# --- artifact deserialization (NO fitting in backend) ---
joblib>=1.3
scikit-learn>=1.4
xgboost>=2.0
imbalanced-learn>=0.12            # imblearn Pipeline is the persisted artifact type
shap>=0.44                        # per-prediction explanations

# --- testing ---
pytest>=8.0
httpx>=0.27                        # FastAPI TestClient transport
